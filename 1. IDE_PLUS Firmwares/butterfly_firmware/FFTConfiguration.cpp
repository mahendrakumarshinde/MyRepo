#include "FFTConfiguration.h"

namespace FFTConfiguration {
    extern int currentSamplingRate = DEFAULT_SAMPLING_RATE;
    extern int currentBlockSize = DEFAULT_BLOCK_SIZE;
    extern int currentLowCutOffFrequency = DEFALUT_LOW_CUT_OFF_FREQUENCY;
    extern int currentHighCutOffFrequency = DEFAULT_HIGH_CUT_OFF_FREQUENCY;
    extern float currentMinAgitation = DEFAULT_MIN_AGITATION;
}